//(function ($, moment, undefined) { // Cápsula

//    $.validator.addMethod('date', function (value, element) {

//        return this.optional(element) || moment(value, 'DD-MM-YYYY').isValid();

//    });

//}(jQuery, moment));